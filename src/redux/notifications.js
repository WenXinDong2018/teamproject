import { NOTIFICATIONS } from '../shared/notifications';

export const Notifications = (state = NOTIFICATIONS, action) => {
    switch (action.type) {

        default:
            return state;
            }
};
