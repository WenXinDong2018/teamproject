import { NEARBYSTORES } from '../shared/nearbystores';

export const NearbyStores = (state = NEARBYSTORES, action) => {
    switch (action.type) {

        default:
            return state;
            }
};
