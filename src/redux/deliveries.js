import { DELIVERIES } from '../shared/deliveries';

export const Deliveries = (state = DELIVERIES, action) => {
    switch (action.type) {

        default:
            return state;
            }
};
